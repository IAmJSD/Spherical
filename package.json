{
  "name": "spherical",
  "version": "1.0.0",
  "description": "A decentralised social media platform.",
  "private": true,
  "scripts": {
    "bundle:dev": "NODE_ENV=development webpack && node ./scripts/normaliseManifset.js",
    "bundle:prod": "NODE_ENV=production webpack && node ./scripts/normaliseManifset.js",
    "start:dev": "yarn run bundle:dev && DEV=true go run ./cmd/spherical",
    "start:prod": "yarn run bundle:prod && DEV=false go run ./cmd/spherical",
    "build": "yarn run bundle:prod && rimraf dist && mkdir dist && node ./scripts/buildBinaries.js"
  },
  "devDependencies": {
    "@types/markdown-it": "^12.2.3",
    "@types/styled-components": "^5.1.26",
    "babel-plugin-styled-components": "^2.0.7",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0",
    "webpack-manifest-plugin": "^5.0.0"
  },
  "dependencies": {
    "markdown-it": "^13.0.1",
    "preact": "^10.11.2",
    "styled-components": "^5.3.6"
  },
  "alias": {
    "react": "preact/compat",
    "react-dom": "preact/compat"
  }
}
